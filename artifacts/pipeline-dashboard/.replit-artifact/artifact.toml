kind = "web"
previewPath = "/"
title = "ssPipelineControl"
version = "1.0.0"
id = "artifacts/pipeline-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25935

[services.development]
run = "pnpm --filter @workspace/pipeline-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pipeline-dashboard", "run", "build" ]
publicDir = "artifacts/pipeline-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25935"
BASE_PATH = "/"
